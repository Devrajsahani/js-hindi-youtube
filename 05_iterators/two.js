let score = 11;
do {
    console.log(`score is ${score}`)
} while (score<=10);
// so here even though the score value is more than the condition given it will print it and then stop because in do while first the element is printed then the condition is checked.